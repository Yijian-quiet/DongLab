# 课题组网站

课题组网站：https://donghaolab.com/  
该网站建立在阿里云服务器上，内容包括课题组介绍、成员介绍、研究方向、发表论文、实验设备等信息。网站定期更新，方便成员和外界了解课题组的最新动态和科研成果。  
网站由课题组成员自行维护和更新，确保信息的准确性和及时性。如有任何建议或需要更新的信息，请联系网站管理员。

## 网站维护

**网站管理员**：张一健
**邮箱**：zhangyijain1023@qq.com

网站基于 WordPress 建站，使用 WordPress 官方插件和主题，可满足大部分网站需求。  
日常更新通过 WordPress 后台进行，网址为：
https://donghaolab.com/wp-login.php

日常更新内容：
- 课题组成员动态更新
- 发表论文动态更新
- 组内合影及新闻
- 通过 wp 后台的「Page」选择对应界面进行更新

同时使用宝塔进行服务器管理，网址：`47.116.131.171:8888/4fe28c31/`

## 部署计算服务

组内部分科研项目（如 RetrosynthesisZero、ChemMixPlanner 等）通过网站对外提供计算服务，例如：
https://donghaolab.com/retro/

在部署服务时需解决以下问题：

### 1. 网络通联

因计算服务器运行在校园内网，而提供服务需要在具备公网 IP 的阿里云服务器上进行，因此需要：

1. **阿里云防火墙** — 开放相应端口用于 frp 内网穿透
2. **校园网认证** — 计算服务器需先登录校园网（可使用自动认证脚本）
3. **frp 内网穿透** — 将内网服务映射到阿里云服务器

### 2. 校园网认证

南京大学校园网认证脚本示例：

```bash
#!/usr/bin/env bash
# 南京大学校园网登录/登出脚本

cer_login() {
    echo -n "Username: "
    read username
    echo -n "Password: "
    read -s password
    curl -d "username=${username}&password=${password}" http://p2.nju.edu.cn/portal_io/login
}

cer_logout() {
    curl http://p2.nju.edu.cn/portal_io/logout
}

main() {
    case "$1" in
        -i) cer_login ;;
        -o) cer_logout ;;
        *)  echo "用法: $0 [-i 登录 | -o 登出]" ;;
    esac
}
main "$@"
```

### 3. frp 内网穿透（新版 toml 配置）

> ⚠️ frp 新版本（v0.52+）已切换为 **toml** 配置格式，旧版 ini 格式已弃用。

**阿里云服务端 frps.toml：**
```toml
bindAddr = "0.0.0.0"
bindPort = 8000

auth.method = "token"
auth.token = "XXX"

allowPorts = [
  { start = 5000, end = 5000 }
]

log.to = "./frps.log"
log.level = "info"
```

**内网服务器客户端 frpc.toml：**
```toml
serverAddr = "阿里云公网IP"
serverPort = 8000
auth.method = "token"
auth.token = "XXX"
loginFailExit = false

[[proxies]]
name = "retrosynthesis"
type = "tcp"
localIP = "127.0.0.1"
localPort = 5000
remotePort = 5000
```

### 4. Nginx 反向代理

不建议直接开放端口，推荐通过宝塔面板添加反向代理。

在宝塔中进入 `donghaolab.com` 的配置文件，添加以下配置：

```nginx
location /retro/ {
    proxy_pass http://127.0.0.1:5000/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 300;  # 延长超时时间（5分钟）
}
```

配置完成后通过 `https://donghaolab.com/retro/` 即可访问。

### 5. 注意事项

- 阿里云服务器仅作为公网入口，所有计算服务均在内网服务器运行
- 服务需直接提供网页界面（如 FastAPI + 前端）才能使用反向代理

## 改进方向

- 增添用户管理系统
- 提供可视化界面
- 条件合适时增添 ChemBart 等模型，提供交互界面
