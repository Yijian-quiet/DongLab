# 课题组网站
课题组网站：https://donghaolab.com/  
该网站建立在阿里云服务器上，内容包括课题组介绍、成员介绍、研究方向、发表论文、实验设备等信息。网站定期更新，方便成员和外界了解课题组的最新动态和科研成果。  
网站由课题组成员自行维护和更新，确保信息的准确性和及时性。如有任何建议或需要更新的信息，请联系网站管理员。

## 网站维护
网站管理员：张一健
邮箱：2949500972@qq.com
电话：17736123717

网站基于WordPress建站，使用WordPress官方插件和主题，可满足大部分网站需求。      
我们的日常更新可以通过WordPress提供的网站后台进行，网址为：https://donghaolab.com/wp-login.php?redirect_to=https://donghaolab.com/wp-admin/&reauth=1  
日常更新内容为：
- 课题组成员动态更新
- 发表论文动态更新
- 组内合影及新闻
通过wp后台的‘Page’选择对应界面进行更新

同时使用宝塔进行服务器管理，网址为：47.116.131.171:8888/4fe28c31/


## 增添计算服务
组内的一些科研项目需要通过网站对外提供计算服务，如：
RetroSynthesisZero: https://donghaolab.com/retro/

在部署该服务时遇到了不少问题，现整理以下经验：
- 网络问题  **阿里云防火墙**  **校园网访问**  

    由于计算服务器运行在校园内网，而提供服务需要在具备公网IP的阿里云服务器上进行，因此首先需要配置阿里云服务器的防火墙，并开放相应的端口。  
    该端口用于部署内网穿透服务frp，而用于服务的端口可不开放而使用ngxinx进行反向代理。

- 校园网访问问题，服务器部署在校园网内，首先需要登录校园网，登录脚本如下
```bash
#!/usr/bin/env bash
################################################################################
# Campus network certification of NJU
# 南京大学校园网登录/登出脚本
# by: Y. YANG (me@yangy.one)
# usage: bash connect.sh [-i | -o]
# comments:
################################################################################

cer_login() {
    echo -n "Username: "
    read username
    echo -n "Password: "
    read -s password

   curl -d "username=${username}&password=${password}" http://p2.nju.edu.cn/portal_io/login </dev/null 2>/dev/null
}

cer_logout() {
   curl http://p2.nju.edu.cn/portal_io/logout </dev/null 2>/dev/null
   #curl -d "logoutBtn" http://p.nju.edu.cn/portal_io/login </dev/null 2>/dev/null
}

usage() {
    cat << EOF >&2
-i : login
-o : logout
-h : usage
EOF
}

main() {
    while getopts ioh arg; do
        case $arg in
            i) cer_login ;;
            o) cer_logout ;;
            h) usage; exit 0 ;;
            *) usage; exit 1 ;;
        esac
    done
    shift $((OPTIND-1))
}

main "$@"
```
将上述网络通信问题解决后便可以部署frp进行内网穿透了。

阿里云端frps
```ini
# frps.ini
# sudo ./frps -c ./frps.toml
bindAddr = "0.0.0.0"
bindPort = 8000

# 启用 token 认证（官方推荐方式）
auth.method = "token"
auth.token = "XXX"

# 可选：开放用于代理的端口范围（确保包含 5000）
allowPorts = [
  { start = 5000, end = 5000 }
]

# 日志
log.to = "./frps.log"
log.level = "info"
```
内网服务器frpc
```ini
# frpc.ini
# sudo ./frpc -c ./frpc.toml
serverAddr = "阿里云公网IP"
serverPort = 8000

# 必须与 frps 一致
auth.method = "token"
auth.token = "XXX"
loginFailExit = false

log.level = "debug"

[[proxies]]
name = "retrosynthesis"
type = "tcp"
localIP = "127.0.0.1"
localPort = 5000
remotePort = 5000
```
这时如果开放了5000端口，可以通过http://阿里云公网IP:5000访问内网服务器。    
但是我们不建议直接开放5000端口，而使用反向nginx进行反向代理。  

服务器后台使用了宝塔管理，可直接进入宝塔后台，进入donghaolab.com的配置文件中添加反向代理，nginx配置如下：
```nginx
# retro 反向代理
location /retro/ {
    proxy_pass http://127.0.0.1:5000/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
        # === 新增：延长超时时间（单位：秒）===
    #proxy_connect_timeout 300;
    #proxy_send_timeout     300;
    proxy_read_timeout     300;  # 关键！默认60秒，改为300秒（5分钟）
}
```
此时通过 http://donghaolab.com/retro/ 即可访问到内网服务器的逆合成服务。  
当然这种配置方法要求该服务本身直接提供网页，当前的逆合成服务使用了fastapi并且配置了前端网页，因此可以直接使用反向代理。     
因此阿里云服务器仅提供了对公网开放的平台，所有计算服务均在内网服务器进行

- 改进方向  
增添用户管理系统  
提供可视化界面  
条件合适可以增添ChemBart等模型，提供交互界面

